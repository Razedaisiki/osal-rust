[workspace]

[package]
name = "osal-integration-freertos-qemu-mps2"
version = "0.1.0"
edition = "2024"
description = "Rust staticlib linked into the OSAL FreeRTOS QEMU MPS2 firmware"

[lib]
crate-type = ["staticlib"]

[dependencies]
osal-backend-freertos-sys = { path = "../../../crates/osal-backend-freertos-sys" }
osal-backend-freertos = { path = "../../../crates/osal-backend-freertos", features = ["integration-test-hooks"] }
osal-api = { path = "../../../crates/osal-api" }
osal = { path = "../../../crates/osal", default-features = false, features = ["backend-freertos"] }

# Portable demos — the very same `osal_demo::<name>::run()` functions the
# POSIX host binaries call. Only the platform shell differs.
osal-demo = { path = "../../../examples/osal-demo", default-features = false, features = ["backend-freertos"], optional = true }

[features]
default = ["suite-aggregate"]
suite-aggregate = []
suite-queue-blocking = []
suite-task = []
suite-timer = []
suite-mixed = []
demo = ["dep:osal-demo"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
